# CarbonCount 环境变量示例（生产部署）
#
# 用法：
#   1) 复制为 .env
#   2) 修改其中的敏感项（POSTGRES_PASSWORD、SECRET_KEY）
#
# 注意：
#   - backend/app/core/config.py 默认会读取项目根目录的 .env
#   - 切勿将真实的 .env 提交到仓库

# -------------------------
# PostgreSQL
# -------------------------
POSTGRES_DB=carboncount
POSTGRES_USER=carbonuser
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# -------------------------
# FastAPI
# -------------------------
# 生产环境容器内通过服务名 db 访问数据库
DATABASE_URL=postgresql://carbonuser:CHANGE_ME_STRONG_PASSWORD@db:5432/carboncount

# JWT/加密密钥：生产务必修改为随机长字符串（至少 32 字符）
SECRET_KEY=CHANGE_ME_RANDOM_SECRET_KEY_AT_LEAST_32_CHARS

# 环境标识（用于日志/调试开关等）
ENVIRONMENT=production
DEBUG=false

# -------------------------
# Web (Nginx) 监听端口
# -------------------------
# 默认将宿主机 80 映射到容器 80。若服务器 80 被占用，可改为 3000/8080 等。
WEB_PORT=80

